document.getElementById('addBlock').addEventListener('click', function() {
    var domain = document.getElementById('domain').value;
    var memeFile = document.getElementById('memeFile').files[0];
    // TODO: Save the domain and meme to storage
    // You can use the FileReader API to read the memeFile
  });
  